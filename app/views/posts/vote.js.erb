<% if @vote.valid? %>
  $("#post_<%=@post.slug%>_votes").html("<%=@post.total_votes%> votes")
<% else %>
$('#vote_error_<%=@post.slug%>').show().html("You can only vote each post once!")
 // alert("You can only vote once on a post")
<% end %>